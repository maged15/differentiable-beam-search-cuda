name: Performance regression
description: Report benchmark regression
labels: [performance]
body:
  - type: textarea
    id: benchmark
    attributes:
      label: Benchmark output
      render: text
    validations:
      required: true
  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: CPU/GPU, driver, CUDA, compiler, PyTorch
    validations:
      required: true
